// Here we use a feature of typescript that simplifies the way 
// how we can create a typical constructor

export class Ingredient {
    constructor(public name: string, public amount: number) {}
}

